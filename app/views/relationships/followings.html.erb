<div class='container'>
  <div class='row'>
      <h2>Follow user</h2>
      <% if @users.exists? %>
        <%= render 'users/index', users: @users %>
      <% else %>
       <p>ユーザーはいません</p>

      <% end %>

  </div>
</div>
